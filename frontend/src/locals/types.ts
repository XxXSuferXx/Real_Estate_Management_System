export interface Translation {
  nav: {
    home: string;
    properties: string;
    services: string;
    contacts: string;
    about: string;
  };
  topBar: {
    login: string;
  };
  hero: {
    title: string;
    description: string;
  };
  footer: {
    rights: string;
  };
}